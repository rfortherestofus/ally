% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agent_instructions.R
\name{edit_agent_instructions}
\alias{edit_agent_instructions}
\title{Open an agent instruction file}
\usage{
edit_agent_instructions(agent = NULL, scope = c("user", "project"))
}
\arguments{
\item{agent}{\code{NULL}, the default, to pick from every listed file, or one of
\code{"claude"}, \code{"codex"}, \code{"posit"} or \code{"agents"}.}

\item{scope}{Where to look: \code{"user"} (your home folder), \code{"project"} (the
working directory), or both, the default.}
}
\value{
Invisibly, the path of the file opened, or \code{NULL} when nothing was.
}
\description{
Opens one of the files \code{\link[=list_agent_instructions]{list_agent_instructions()}} shows, in Positron or
RStudio when you are working in one, or in R's own editor otherwise.
}
\details{
Run it with no arguments to pick from a numbered list. Or name the file by the
agent that reads it: \code{"claude"} for Claude Code, \code{"codex"} for Codex, \code{"posit"}
for Posit Assistant, or \code{"agents"} for the project's shared \code{AGENTS.md}. When
that still matches more than one file (Claude Code has one for you and one for
the project, for instance), you pick from a short list, or narrow it with
\code{scope}.

If the file does not exist yet, you are asked whether to create it, empty.
Existing files are only opened, never changed.
}
\examples{
\dontrun{
# Pick from a numbered list
edit_agent_instructions()

# Claude Code's instructions for every project
edit_agent_instructions("claude", scope = "user")

# The project's AGENTS.md
edit_agent_instructions("agents")
}
}

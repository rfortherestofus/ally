% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agent_instructions.R
\name{list_agent_instructions}
\alias{list_agent_instructions}
\title{List agent instruction files}
\usage{
list_agent_instructions(scope = c("user", "project"))
}
\arguments{
\item{scope}{Where to look: \code{"user"} (your home folder), \code{"project"} (the
working directory), or both, the default.}
}
\value{
Invisibly, a tibble with one row per file, in the order shown, and
columns:
\itemize{
\item \code{scope}: \code{"user"} or \code{"project"}.
\item \code{agent}: the name \code{\link[=edit_agent_instructions]{edit_agent_instructions()}} takes for the file:
\code{"claude"}, \code{"codex"}, \code{"posit"} or \code{"agents"}.
\item \code{read_by}: the agents that read the file.
\item \code{path}: the full path to the file.
\item \code{exists}: whether the file exists.
\item \code{lines}: how many lines the file has, or \code{NA} when it does not exist.
}
}
\description{
Shows the instruction files your AI coding assistants read: the Markdown
files where you tell an agent how you like to work (\code{CLAUDE.md} for Claude
Code, \code{AGENTS.md} for Codex, Posit Assistant and others). Instructions can
apply to every project on the computer (\code{"user"}, in your home folder) or to
one project (\code{"project"}, in the working directory).
}
\details{
The main file for each agent is always listed, marked "not found" when it
does not exist yet, so you can see where it would go:
\itemize{
\item User: \verb{~/.claude/CLAUDE.md} (Claude Code), \verb{~/.codex/AGENTS.md} (Codex)
and \verb{~/.posit/assistant/AGENTS.md} (Posit Assistant). Only agents
installed on this computer are listed, judged by whether their folder
(\verb{~/.claude}, \verb{~/.codex}, \verb{~/.posit}) exists.
\item Project: \code{AGENTS.md}, read by Codex and Posit Assistant, and by Claude
Code when the project has no \code{CLAUDE.md}; and \code{CLAUDE.md}, read by Claude
Code.
}

Less common files are listed only when they exist: \verb{~/.codex/AGENTS.override.md},
\code{.claude/CLAUDE.md} and \code{CLAUDE.local.md}.

If a project has both an \code{AGENTS.md} and a \code{CLAUDE.md}, Claude Code reads only
the \code{CLAUDE.md}, and a note says so. A \code{CLAUDE.md} that contains the line
\verb{@AGENTS.md} pulls the \code{AGENTS.md} in, so there is no note.

Nothing is created or changed. Use \code{\link[=edit_agent_instructions]{edit_agent_instructions()}} to open a file.
}
\examples{
\dontrun{
list_agent_instructions()
list_agent_instructions(scope = "user")
}
}
